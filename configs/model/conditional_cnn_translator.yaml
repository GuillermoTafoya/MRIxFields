name: identity
encoder:
  name: identity
decoder:
  name: identity
translator:
  name: conditional_cnn_field_translator
  in_channels: 1
  out_channels: 1
  hidden_channels: [16, 32]
  latent_channels: 32
  cond_dim: 64
  spatial_dims: 2
  upsample_mode: interpolate
  activation: silu
  final_activation: null
